# Build artifacts
/tshare
/dist/

# Runtime state / inboxes (created by `tshare -u` / folder shares)
/tshare-inbox/
*.enc

# macOS / editor / sync cruft
.DS_Store
.stfolder/
*.sync-conflict-*

# local app checkouts (third-party repos run natively by --room / --kuma)
/mirotalk/
